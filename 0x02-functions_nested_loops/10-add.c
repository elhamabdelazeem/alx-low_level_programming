#include"main.h"
#include<unistd.h>
/**
 * add - adds two integers and returns the result
 * @a: first operand
 * @b: second operand
 * Return: result of addition
 */
int add(int a, int b)
{int sum;
sum = a + b;
return (sum); }
