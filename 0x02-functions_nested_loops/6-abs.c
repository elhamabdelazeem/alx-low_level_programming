#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 */
int _abs(int)
{int absolute, n;
if (n > 0)
{
absolute = 1* n;
return (absolute);
}
else if (n < 0)
{
absolute = -1*n;
return (absolute);
}
return (0); }
